package ru.netology;

import java.util.ArrayList;

public class Game {
    public boolean registered;
    ArrayList<String> registeredPlayer = new ArrayList<>();
    if (registered) {
        registeredPlayer.add(player.name);
    }

}
